#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/*
 * print_array - prints n elements of an array
 *
 * return: void
 *
 */void print_array(int *a, int n)
{
	int i;

	for (i = 0; i <= n; i++)
	{
		printf("%d", s[i];

		while (i != n)
		{
			printf(", ");
		}
	}
}
